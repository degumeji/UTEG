export interface MenuItem {
  name: string;
  path: string;
  icon: string;
  subPaths: string[];
}

export interface LanguageOption {
  value: string;
  label: string;
  shortLabel: string;
  icon: string;
}
